#pragma once
#include <string>

void printBoxedText(const std::string &text, const std::string &color = "",
                    std::size_t padding_x = 6, std::size_t padding_y = 3,
                    std::size_t margin_x = 2, std::size_t margin_y = 1);
